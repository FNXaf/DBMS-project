1. First, Open MySQL Workbench, and connect to the profile.
    - Check if the server is running or not :
        - Click on Server
        - Click on Server Status
        - Must say "Running"

2. in VS Code, open terminal (ctrl + ~) enter:
    - npm run dev:backend

3. in VS Code, open new terminal (ctrl + shift + ~) enter:
    - npm run dev:frontend
    